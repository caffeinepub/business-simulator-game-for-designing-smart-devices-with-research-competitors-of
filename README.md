## Business simulator game for designing smart devices with research, competitors, office upgrades, and cloud saves

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
